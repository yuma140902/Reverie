name: Rust

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -Dwarnings
  RUSTDOCFLAGS: -Dwarnings
  SCCACHE_GHA_ENABLED: "true"
  RUSTC_WRAPPER: "sccache"

jobs:
  build:
    strategy:
      matrix:
        os: [ windows-latest, ubuntu-latest, macos-14 ]
        cmd:
          - build --all-targets --workspace
          - build --all-targets --workspace --no-default-features
          - build --all-targets --workspace --no-default-features --features raw_gl_context
          - build --all-targets --workspace --no-default-features --features raw_gl_context,glutin
          - test --all-targets --workspace --all-features
          - clippy --all-features --workspace -- '-Dclippy::all' -Dwarnings '-Dclippy::nursery'
          - doc --no-deps --workspace --document-private-items
        release: [ --release, '' ]
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v4
    - uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.toml') }}
        restore-keys: |
            ${{ runner.os }}-cargo-
    - uses: mozilla-actions/sccache-action@v0.0.5
    - uses: dtolnay/rust-toolchain@stable
    - run: cargo ${{ matrix.cmd }} ${{ matrix.release }}
